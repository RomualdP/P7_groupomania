export default function createNewPost() {
    let createnewpost = Boolean
    if (createnewpost === false) {
        createnewpost = true
    } else { createnewpost = false }
    return (createnewpost)
}

